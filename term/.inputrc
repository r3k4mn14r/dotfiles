# vi mode everywhere
set editing-mode vi

# vi settings                                         
$if mode=vi
    # jk for insert mode             
    set keymap vi-insert
    "jk": vi-movement-mode # remap escape
$endif
